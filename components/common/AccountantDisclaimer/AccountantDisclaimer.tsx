import styles from './AccountantDisclaimer.module.css';

const AccountantDisclaimer = () => {
  return (
    <section className={styles.container}>
      Apelați la un contabil! Informațiile de pe acest site există doar pentru a
      avea o privire de ansamblu
    </section>
  );
};

export default AccountantDisclaimer;
